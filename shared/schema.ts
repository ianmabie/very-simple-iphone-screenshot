// This file contains shared types and schemas between frontend and backend
// Currently, the app doesn't require any persistent data storage

export interface AppInfo {
  name: string;
  version: string;
  status: string;
}
